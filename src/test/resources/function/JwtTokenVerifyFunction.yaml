openapi: 3.0.1
info:
  title: testing source file
  description: application info
  version: function
servers:
  - variables:
      functionName:
        default: JwtTokenVerify
      classPath:
        default: org.huasuoworld.security.JwtTokenVerify
      input:
        default: 'AppToken'
      output:
        default: 'AppToken'
components:
  schemas:
    AppToken:
      required:
        - token
      type: object
      properties:
        token:
          type: string
          example: test